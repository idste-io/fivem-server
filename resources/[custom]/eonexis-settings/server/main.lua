-- eonexis-settings — server
-- Settings are stored client-side via KVP, so the server is minimal.
-- This exists to keep the resource structure consistent and to expose the
-- webapp link helper to other server resources if needed.

print('[eonexis-settings] loaded — client-side KVP settings, webapp link ready')
